import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['travel-journal-api-bootcamp.do.dibimbing.id','s-light.tiket.photos','media.kompas.tv'],
  },
};

export default nextConfig;
